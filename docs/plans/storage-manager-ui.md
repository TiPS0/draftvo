# Storage Manager UI

> **Generated by `/agent-plan`**
> **Date:** 2026-08-20
> **Type:** new_feature

---

## 1. Summary

Add a new "Storage" section to the Settings Modal. This section will feature a non-technical, user-friendly Storage Manager UI containing a visual progress bar (showing used vs. free space) and simple controls to configure where data is stored.

## 2. Affected Screens & Components

- **[MODIFY]** `frontend/src/app/_components/SettingsModal.tsx` — Add the new navigation item for "Storage" and build the Storage UI view when the tab is active.

## 3. User Flow (Step-by-Step)

1. User clicks on "Settings" to open the `SettingsModal`.
2. In the left sidebar, under the "Workspace" section, the user clicks the new "Storage" tab.
3. The main content area updates to show:
   - A title: "Storage Manager".
   - A visual, multi-colored progress bar indicating storage usage (e.g., "Drafts", "Other", "Free space").
   - A breakdown text of the storage amounts (e.g., "1.5 GB Used • 86.9 GB Free").
   - A simple settings block allowing the user to select their storage location (e.g., "Local Browser Storage" vs "Cloud Sync").

## 4. Technical Requirements

### Data & State
- Add `'storage'` to the possible values of `activeTab`.
- For the initial implementation, use **mock data** for the storage capacities (e.g., 200GB total, 86GB free) so the UI can be styled and tested.

### Layout & Components
- Add a new `DatabaseIcon` or `HardDriveIcon` for the sidebar.
- Implement the `activeTab === 'storage'` view block.
- The storage progress bar should be built using simple `div` elements with percentage widths (e.g., `w-[15%]`, `w-[60%]`) and distinct background colors.

## 5. Design & UX Notes

- **Non-tech Friendly:** Avoid overly complex file system paths. Use clear, conversational labels like "Your device" or "Cloud".
- **Visuals:** Use bright, friendly colors for the progress bar (e.g., blue for Drafts, yellow for Other, gray for Free space) similar to modern OS storage managers, but utilizing the existing CSS variables where possible.
- **Spacing:** Maintain the exact same header margins (`mb-10`, `pb-6`, etc.) used in the `Preferences` tab for consistency.

## 6. Hard Constraints (DO NOT CHANGE)

- Do not alter the existing `Preferences` tab logic or layout.
- Maintain the modal's `max-w-[1050px]` and scrolling behavior.
- Keep all styling using Tailwind utility classes and CSS variables (e.g., `var(--color-surface-raised)`).

## 7. Success Criteria

The task is complete when:

- [ ] A new "Storage" tab appears in the Settings sidebar.
- [ ] Clicking it reveals the Storage Manager with a visual progress bar.
- [ ] `pnpm tsc --noEmit` passes with 0 errors.
