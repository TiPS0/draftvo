# Fix Auth Issues & Navigation

> **Generated by `/agent-plan`**
> **Date:** 2026-08-14
> **Type:** bug_fix

---

## 1. Summary

Fix the hydration error caused by browser extensions, correct the back button behavior after login, remember the last logged-in email, and verify the route guard.

## 2. Affected File(s)

- **[MODIFY]** `frontend/src/app/layout.tsx` — Add `suppressHydrationWarning` to the `<body>` tag to prevent mismatch errors from browser extensions injecting attributes (like `cz-shortcut-listen`).
- **[MODIFY]** `frontend/src/app/(auth)/login/page.tsx` — Change `router.push("/")` to `router.replace("/")` so the login page is removed from the history stack. Add `localStorage` logic to save and restore the last used email.
- **[MODIFY]** `frontend/src/app/(auth)/register/page.tsx` — Change `router.push("/login?registered=1")` to `router.replace("/login?registered=1")`.

## 3. Change Description

1. **Hydration Error:** In `frontend/src/app/layout.tsx`, update the body tag to `<body className="min-h-full flex flex-col" suppressHydrationWarning>`.
2. **Back Navigation:** In `login/page.tsx` and `register/page.tsx`, replace instances of `router.push` with `router.replace` when navigating away after successful submission.
3. **Remember Email:** In `login/page.tsx`:
   - Initialize the `email` state by reading from `localStorage.getItem("lastLoginEmail")` inside a `useEffect` (since it's a client component).
   - On successful login, call `localStorage.setItem("lastLoginEmail", values.email)`.
4. **Proxy Verification:** Confirm that `frontend/src/proxy.ts` correctly blocks access to `/` for unauthenticated users. (The current implementation is correct, but needs manual validation).

## 4. Hard Constraints

- Do not change the overall UI design tokens.
- Keep the JWT and proxy logic intact.

## 5. Done When

- [ ] `suppressHydrationWarning` is present on the body tag and the hydration console error is gone.
- [ ] After logging in and landing on `/`, pressing the browser's back button does NOT return to the login form.
- [ ] The email field is prepopulated with the last successful login email.
- [ ] Accessing `http://localhost:3000/` without a session automatically redirects to `/login`.
- [ ] `pnpm tsc --noEmit` passes with 0 errors.
